# Core workflow engine logic: validates and executes state transitions
# Checks that the requested transition exists for the current state,
# that the user has the required role, then updates task state and writes audit log
